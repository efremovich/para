# 2026-04-22

## operator-api-gateway (Cursor)
- Разобрано сообщение лога про `Route matched` / `Executing controller action` для `AddressBookController.GetContacts`: это встроенное логирование ASP.NET Core MVC, не код репозитория; не является причиной 500.
- Указаны места в коде: маршрут `GET api/v1/addressBook/invitations/contacts`, вызов `InvitationsApiClient.GetContacts` с `EnsureSuccessStatusCode`.

- Анализ 500 на stage для `addressBook/invitations/contacts` и `incoming`: Ocelot проксирует на `address-book-api`; типичные причины — ответ оператора не 2xx, десериализация, `ParseContactStatus` при новом значении `status` (только цепочка GetContacts).
