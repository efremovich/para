# 2026-04-27

## operator-api-gateway / address-book-api
- По логам stage: `operator-stage` отвечает **504** за ~2008 ms на `GET .../invitations/incoming` и `contacts`; `InvitationsApiClient.EnsureSuccessStatusCode()` → `HttpRequestException` → клиенту уходит **500**.
- В запросе к оператору передаётся `gate_id=00000000-0000-0000-0000-000000000000` (нет `abonentId` в query) — проверить конфиг `OperatorGateId` на stage.
