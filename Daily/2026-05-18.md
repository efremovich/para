
## Edi.Operator — пулы БД / pgbouncer

Исправлен wpgx: MaxConns из pool_size (не 1000), V1 без Ping на hot path, Sqlx через OpenDBFromPool (один пул на DSN). astral/roseu/reg/build переведены на wpgx.Sqlx; метрики Prometheus edo_operator_wpgx_pool_*; Helm maxSurge:1 и комментарии бюджета conn.

## operator-api / operator-edi k8s scale

- operator-api: fatal "terminated" — SIGTERM при shutdown, не БД; исправлен http_server (graceful exit).
- Убрано дублирование wpgx (pg как "" и "pg"), ReLoadDBConn единый, pool_size из конфига.
- Ранний HTTP для startup probe в build/main.

## operator-api — cheap_ping

Восстановлен `/system/cheap_ping` (и trailing slash) через `http_server.RegisterCheapPing()` в блоке system routes; убран из раннего probe-блока (там остались только `/health/startup` и `/health/ready`).

## operator-edi — SIGTERM при scale roseu

Лог подтвердил штатный SIGTERM (не fatal). Исправления: ранний HTTP с `/health/startup`, `/health/live` (без БД); readiness на operator+q; `health.New` в main; liveness в Helm → `/health/live`; `terminationGracePeriodSeconds: 90`; drain NATS-очередей 60s; `wpgx.Ping` без pool.Reset в healthcheck.

## operator-api — ReloadOperators SQL 42601

Причина: `ReLoadDBConn` вызывал `wpgx.ClearV1()`, терялись запросы из `init()` (users/sql.go); в БД уходил sha1-ключ как SQL → `7e0e` syntax error. Убран ClearV1 из reload; защита resolveStmt в wpgx.

## Массовая смерть 60 edi + 60 roseu-packer

Причина: исчерпание pgbouncer + liveness с проверкой БД на всех pod. Исправления: roseu `/health/live` без БД, ранний fiber Listen, InitHealth; perf WPGX_POOL_SIZE=2; скрипт `scripts/capture-operator-pod-death.sh`.

## Edi.Operator — operator-edi perf-test

- Исправлен `wpgx.Connect`: `WPGX_POOL_SIZE` / `countPool` задаёт `pgxpool.MaxConns` (раньше всегда 1000).
- Убран Ping на каждый вызов `wpgx.V1()` (hot path).
- Разделены K8s probes: `/health`, `/health/startup` — liveness; `/health/ready` — проверки БД.
- perf-test: `replicas: 1` вместо 0.

- Readiness 503: PingConn вместо SELECT, MinConns=1 при MaxConns>=2, смягчён readiness probe в perf-test.

- Стабилизация probes под нагрузкой (edi/api/roseu): lenient PingConn, sequential DB check, раздельные liveness/ready; фикс roseu healthChecks pointer.
