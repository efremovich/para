- В `astral/writer.go` исправлено преобразование документов в УС в `MakeContainerEDIGPB`: результат конвертации теперь записывается обратно в `w.packet` по индексу, чтобы упаковка действительно шла с УС-документами.
- Проверена компиляция: `go build ./astral` проходит успешно.
- В `astral/writer.go` убрано дублирование логики конвертации ИОП/УОУ в УС: выделен общий метод `convertDocToUniversalMessage` и подключен в `MakeContainer` и `MakeContainerEDIGPB`.
- Поведение сохранено, сборка `go build ./astral` успешно проходит.
