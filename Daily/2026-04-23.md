# 2026-04-23

## Работа (Edi.Operator)

- Повторное ревью после правок разработчика: переименование `RoseuClient` → `ReprocessClient`, конфиг `reprocess_api_url` / `reprocess_ports`, нейтральные тексты и swagger; убраны `ReprocessGateway` и заглушка `DetermineSubtopic`, в репроцессоре проверка `determineSubTopic != nil`.
- Остаётся: в `SendReprocessByErrCodeRequest` по-прежнему неверная логика `not_later_than` при ненулевом времени; `getDataType` без защиты от `nil`; `whereCreatedNotLaterThan` без изменений.
