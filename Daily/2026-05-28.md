# 2026-05-28

- В `fnstech` добавлена поддержка сущности Drafts в `entity.Message`.
- Реализованы мапперы `DraftToFnsModel` и `DraftToEntity`, подключены в конвертацию `Message <-> MessageDescription`.
- Обновлены тесты `internal/convert/models_test.go` и `parsers/parsers_test.go` для проверки чтения и round-trip черновиков.
- Таргетные тесты по drafts прошли успешно.

- После изменения `EditableData` на `map[string]string` синхронизированы тесты `convert` и `parsers`.
- Исправлена инициализация map в `DraftToEntity`, обновлен формат `description_4.xml` (Название/Значение).
- Проверка: таргетные тесты на drafts зелёные; в `internal/convert` остаются старые падения из-за отсутствующих `testdata/message.xml` и `testdata/invitation.xml`.
- Формат черновика возвращён к виду с текстовым содержимым `РедактируемоеПоле` (chardata), без атрибутов.
- Обновлены DTO/конвертация/тесты под XML вида `<РедактируемоеПоле>...</РедактируемоеПоле>`.
- Таргетные тесты drafts проходят, в пакете `internal/convert` остаются отдельные старые ошибки по отсутствующим testdata-файлам.
- После изменений в models (EditableData -> []string) обновлены тесты `internal/convert/models_test.go` и `parsers/parsers_test.go` под новый тип.
- Таргетные тесты на drafts снова проходят; в `internal/convert` остаются отдельные падения из-за отсутствующих testdata (`message.xml`, `invitation.xml`).
- Рефакторинг читаемости Draft-конвертеров: вынесены helper-функции `editableFieldsToDTO` и `editableFieldsFromDTO`.
- `DraftToFnsModel`/`DraftToEntity` упрощены, убрана прямая вложенность `EditableData.EditableFields` в телах мапперов.
- Таргетные тесты drafts зелёные; в `internal/convert` сохраняются старые падения из-за отсутствующих testdata-файлов (`message.xml`, `invitation.xml`).

- **Edi.Operator (OEDO-9021):** интеграция `Draft` в `entity.LS` — `xmlLS.Drafts`, marshal/unmarshal, учёт черновиков в `LS.Empty()`.
- Тесты `entity_document_draft_test.go` на данных из `test_files/description_draft.xml`: разбор ЛС, marshal/unmarshal и round-trip через `memfile.MakeXML`.
- Исправлен корневой XML-тег при marshal `Draft` (`Черновик` вместо `Draft`).

- **Edi.Operator (OEDO-9177):** ревью ветки `message2docs` — базовый репозиторий `ls2docs`, интеграционный `TestRepo`; найдены: отсутствие `postgresdb` в go.mod (тесты не компилируются), опечатка в README, мёртвый `sql.ErrNoRows` в Delete, нет тестов на валидацию пустых ID.