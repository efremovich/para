
## Edi.Operator — отказ от edicore/lib/zipper
- `checkZipContainsExecutable`: список имён в ZIP через `archive/zip` (`zip.NewReader`), поведение при ошибке разбора — по-прежнему `return nil`.
- Тесты: общий хелпер `testZipArchiveBytes` в `security/zip_testutil_test.go` вместо `zipper.Archive`/`Zip`.
- Зависимость `git.astralnalog.ru/edicore/lib/zipper` убрана из `go.mod`. Файл `Жц` в дереве не найден.
