# 2026-03-29

- Nix/Home Manager: `mpd-mpris` переведён на пакет из `nixpkgs-unstable` (0.4.3) — обновление метаданных для потоков/радио в MPRIS; убран несуществующий `extraConfig` и несуществующие CLI-флаги формата метаданных.

- Waybar: настроены `custom/mpd` (JSON из mpc status + подсказка), `custom/mpd-prev` / `custom/mpd-play-pause` / `custom/mpd-next`, стили в `style.css`; пустой встроенный `mpd = {}` убран.
