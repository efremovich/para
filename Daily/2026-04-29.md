# 2026-04-29

## Edi.Operator

- Исправлен fallback загрузки файла в `File` (`api_v1.go`): ранние выходы, успех после `LoadFileFromStorageV2`, 500 для нетипичных ошибок v2, 404 только при `ErrFileNotFound`.
- В `LoadFileFromStorageV2` отсутствие файла в v2 возвращается как `%w` от `ErrFileNotFound`, чтобы `errors.Is` в API работал корректно.
