# 2026-05-04

## Edi.Operator (OEDO-9253)

- Валидация вложений неформализованных документов (тип + `NeformalType`): белый список расширений, запрет `.bin` вне архива, обход zip/rar/7z/gzip (+ tar в gzip без имени), проверка пароля zip (флаг), ошибки rar/7z при шифровании.
- `SecurityError.FnsErrCode`, ТК: опциональный XML-тег `КодОшибки` в `ErrorReceipt`, значение `010102 – файл непредусмотренного расширения`.
- Те же проверки в `astral` (`Receiver`, `ReceiverPack`) после сохранения, как в `roseu`.
- Зависимости: `github.com/mholt/archiver/v3`, `github.com/bodgit/sevenzip`.

- `security/testdata/`: фикстуры zip/gz/gzip/7z/rar (валидные и с паролем / с exe), `testdata/gen` для пересборки, тест `testdata_archives_test.go` с `//go:embed`.

## Edi.Operator / OEDO-9253
- Миграция разбора неформализованных вложений: `archiver/v3` → `mholt/archives` (`Zip`/`Rar`/`SevenZip` + `Extract`).
- Исправлен `walkUnformalizedZip`: ошибка `Extract` обрабатывается после колбэка (раньше был сломанный `return` + недостижимый код).
- `go.mod`: убраны прямые `mholt/archiver/v3` и `bodgit/sevenzip`; добавлен прямой `github.com/mholt/archives`; `bodgit/sevenzip` остаётся transitive через archives.
- `go test ./security/...` — ок.
