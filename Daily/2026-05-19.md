
## Edi.Operator
- Разбор коммита `397d1b0f` (healthcheck баз и соединений): K8s probes, PingConn, MayFail, лимиты пула wpgx.

## Edi.Operator (продолжение)
- Завершён рефакторинг wpgx: `cfg.MaxConns = countPool`, тесты MaxConns/MinConns.
- build: `dbConnect` — явные пулы `""`, `q`, `u`, `m` + агрегированный healthcheck; убран цикл по Databases.

## Edi.Operator
- Исправлена недоступность сервиса при периодическом `reloadOperatorROSEU` (каждые 10 мин): устранена гонка на `app.OperatorsROSEU` — copy-on-write + `sync.RWMutex`, одна фоновая goroutine через `sync.Once`, отдельный HTTP-клиент для Roaming API, accessors `OperatorsROSEU()` / `GetOperatorROSEU()` для читателей.

## Edi.Operator — operatorsroseu
- Вынесен кэш операторов РОСЭУ в `config/operatorsroseu`: фоновый reload (DB + Roaming API), `sync.RWMutex`, `Get`/`Snapshot` (value-копии), `Start` с `sync.Once` + ticker 10m.
- Убрано поле `OperatorsROSEU` из `Config`; `config/operator.go` — только YAML-операторы и alias `OperatorROSEU`.
- Call sites: `docflow/inv`, `docflow/sys`, `migrator/handlers`.
