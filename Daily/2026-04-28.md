# 2026-04-28

- Разобран PKCS#7/CMS файл `ef5921e800084661b755f7e9d78b1706.p7s`: строгий PKCS#7 и Go `pkcs7` не парсят; OpenSSL CMS показывает SignedData ГосСУОК (BY) с неизвестными OID алгоритмов; подпись отсоединённая (`eContent` отсутствует). Вывод: проверка только специализированным ПО + исходный файл.

- Ответ про национальные OID: извлечение через `openssl asn1parse`, сопоставление со СТБ/реестрами; `go mod vendor`; ограничение — `crypto/x509` не парсит СТБ-ключи, одних OID в pkcs7 недостаточно.

- В `/tmp/testsign/national.oid` добавлен список OID для `openssl asn1parse -oid` (без `#` в начале — иначе OpenSSL может не подхватить строки).

- Сравнение двух BY-подписей: старая — в `certificates` после X.509 идёт CHOICE с тегом A2 (не цепочка из SEQUENCE Certificate); `crypto/x509.ParseCertificates` падает. Новая — один сертификат; версия SignedData 4 vs 1.

- Проверка пары документ (XML `e81548…`) + откреплённая подпись `ef5921…`: `openssl cms -verify` падает на СТБ-ключе; SHA256 файла не совпал с messageDigest в подписи (ожидаемо при алгоритме СТБ).

- Запрошен `github.com/centrifugal/gost`: репозиторий на GitHub отсутствует (404), `go get` не проходит.

- Протестирован `github.com/ddulesov/pkcs7` в `/tmp/testsign/main.go` на `ef5921…`: ParseCMS падает на CertificateChoices (тег A2 после первого серта); main.go обновлён с явным выводом.
