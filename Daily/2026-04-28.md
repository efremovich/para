# 2026-04-28

- Разобран PKCS#7/CMS файл `ef5921e800084661b755f7e9d78b1706.p7s`: строгий PKCS#7 и Go `pkcs7` не парсят; OpenSSL CMS показывает SignedData ГосСУОК (BY) с неизвестными OID алгоритмов; подпись отсоединённая (`eContent` отсутствует). Вывод: проверка только специализированным ПО + исходный файл.

- Ответ про национальные OID: извлечение через `openssl asn1parse`, сопоставление со СТБ/реестрами; `go mod vendor`; ограничение — `crypto/x509` не парсит СТБ-ключи, одних OID в pkcs7 недостаточно.

- В `/tmp/testsign/national.oid` добавлен список OID для `openssl asn1parse -oid` (без `#` в начале — иначе OpenSSL может не подхватить строки).
