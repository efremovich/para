# 2026-03-25

- Настроен запуск 1С (`1cestart`) на NixOS через `buildFHSEnv`: CUPS, GTK/WebKit-стек, явный `libsoup_2_4`, WebKitGTK из отдельного входа `nixpkgs_22_11` (libsoup 2, без конфликта с libsoup 3), `LD_LIBRARY_PATH` с `/usr/lib64` и `/usr/lib`.
- В `flake.nix` добавлен input `nixpkgs_22_11`, модуль `home-manager/modules/1c.nix`, подключение в `default.nix`, обновлён `flake.lock`.

- Добавлен NixOS-модуль `nixos/modules/hasp.nix`: автогенерация `/opt/1cv8/conf/nethasp.ini` для сетевого ключа HASP (`NH_SERVER_ADDR=51.254.148.241`) и one-shot сервис установки runtime из архива `aksusbd-*.tar.gz` через `./dinst`.
- Модуль подключен в `nixos/modules/default.nix` и включен для хоста `chicago` в `hosts/chicago/configuration.nix`.
- Конфигурация `nixosConfigurations.chicago` успешно собирается (`nix build ...system.build.toplevel`).
- Исправлена ошибка `hasp-runtime-install.service` (status=126, `./dinst: /bin/bash: bad interpreter`): в модуле `hasp.nix` добавлен патч shebang `#!/bin/bash` -> `${pkgs.bash}/bin/bash` для файлов из архива и запуск `dinst` через `${pkgs.bash}/bin/bash`.
- Проверка: `nixosConfigurations.chicago` снова успешно собирается после фикса.
- Исправлен запуск 1С в FHS-окружении: добавлен `iproute2` и совместимый `/sbin/ip` (симлинк на `${pkgs.iproute2}/bin/ip`), чтобы убрать ошибку `/bin/sh: /sbin/ip: No such file or directory`.
- Попытка установки `1c_edt_distr_2025.2.3_30_linux_x86_64`: дистрибутив корректный, но `1ce-installer-cli` требует запуск с правами администратора (без sudo установка не выполняется).
- Подтверждена установка `1c_edt_distr_2025.2.3_30_linux_x86_64`: найден desktop launcher `1CEDT Start` и бинарник `/opt/1C/1CE/components/1c-edt-start-0.9.0+229-x86_64/1cedtstart`.
- По логу EDT выявлена причина падения: `libglass.so` не находит `libX11.so.6`. В `home-manager/modules/1c.nix` добавлена FHS-обёртка `1cedtstart` с X11/GTK/JavaFX-зависимостями и запуском `/opt/1C/1CE/components/1c-edt-start-0.9.0+229-x86_64/1cedtstart`.
- Для EDT устранена ошибка `NoClassDefFoundError javafx/...`: в обёртке `1cedtstart-inner` принудительно задана JVM из дистрибутива (`JAVA_HOME=/opt/1C/1CE/components/axiom-jdk-full-17.0.16+12-x86_64`) и запуск через `-vm $JAVA_HOME/bin/java`.
- Исправлен EDT `OutOfMemoryError` по direct buffer memory: в `1cedtstart`-обёртке добавлен `JAVA_TOOL_OPTIONS=-XX:MaxDirectMemorySize=256m` (вместо фактического лимита 16m из ini).
