# 2026-03-24

## Работа (transgorobot)

- Изучено логирование в `gpn-online.vip.go` (logrus + `NewRotatingLogger`, структурированные поля, длительность запросов).
- В `newppr.go`: добавлены `newpprLogger`, хелпер полей, логи HTTP в `sendRequestPPR` (duration, status, тело запроса с маскировкой token), авторизация, транзакции (ЛК и public API), смена лимитов/статусов.
- В `newppr_api.go`: `newpprAPILogger`, логи в `sendRequestPPRAPI` и в сценариях смены статуса/лимитов.
