# 2026-02-02

## Исправление ошибки в коде 1C

Исправлена ошибка компиляции в модуле `FuelAppAPIRabbitMQ.bsl`:
- Заменено использование несуществующего типа `КодированиеДанных` на встроенные функции 1C
- Для кодирования в Base64: `Base64Строка(ПолучитьДвоичныеДанныеИзСтроки(...))`
- Для декодирования из Base64: `ПолучитьСтрокуИзДвоичныхДанных(Base64Значение(...), ...)`
- Исправлено в трех местах: HTTP аутентификация (2 места) и декодирование payload сообщений из очереди
## Оптимизация модуля FuelAppAPIУтилиты

Оптимизирован модуль `FuelAppAPIУтилиты.bsl` с использованием встроенных функций 1C:
- Заменены кастомные реализации `ЗаписатьJSON()` и `ПрочитатьJSON()` на встроенные функции языка
- Добавлены вспомогательные функции `ПредобработатьДляJSON()` и `ПостобработатьИзJSON()` для сохранения логики преобразования дат в RFC3339
- Упрощен код: удалено ~100 строк кастомной реализации, используются стандартные функции 1C
- Сохранена обратная совместимость: все существующие вызовы `ПреобразоватьВJSON()` и `ПреобразоватьИзJSON()` работают без изменений## Добавление функции преобразования JSON в дерево значений

Добавлена функция `ПреобразоватьJSONВДеревоЗначений()` в модуль `FuelAppAPIУтилиты.bsl`:
- Преобразует JSON строку в дерево значений для визуализации и анализа данных
- Поддерживает все типы JSON: объекты, массивы, примитивы
- Рекурсивно обрабатывает вложенные структуры
- Форматирует значения для лучшей читаемости (даты, булевы значения)
- Использует встроенные функции 1C (`ПрочитатьJSON`, `ЗаписатьJSON`)
- Исправлена проверка пустой даты в функции `ПреобразоватьДатуВTimestamp()`## Реализация выгрузки контрактов и контрагентов

Создан модуль `FuelAppAPIДоговоры.bsl` для работы с контрактами:
- Методы получения контрактов: по ID, список с пагинацией, контракты компании
- Методы управления контрактами: создание, обновление, удаление
- Работа с дополнительными данными контрактов
- Работа с типами контрактов (получение по ID и коду)
- Функция `ВыгрузитьКонтракты()` - выгрузка всех контрактов с пагинацией и поддержкой callback
- Функция `ВыгрузитьКонтрагенты()` - выгрузка всех компаний (контрагентов) с пагинацией и поддержкой callback
- Поддержка фильтрации по компании при выгрузке контрактов
- Поддержка поиска при выгрузке контрагентов## Дополнение выгрузки контрагентов заполнением реквизитов

Дополнена функция `ВыгрузитьКонтрагенты()` в модуле `FuelAppAPIДоговоры.bsl`:
- Добавлено заполнение реквизитов для каждой компании при выгрузке (аналогично строке 170 в `FuelAppAPIЗаявки.bsl`)
- Каждая компания дополняется метаданными выгрузки: `exported_at` (время выгрузки в формате RFC3339) и `export_page_number` (номер страницы)
- Формируется структура результата страницы с заполненными реквизитами перед передачей в callback
- Улучшена структура данных, передаваемых в callback процедуру