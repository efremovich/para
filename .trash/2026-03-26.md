# 2026-03-26

## fuel-app-backend

- Переименование доменной сущности **emitent → vendor** по цепочке: proto (`Vendor`, `VendorService`, HTTP `/v1/vendors`), сгенерированный `pkg/api`, entity (`Vendor`, `VendorPeriod`, поля `vendor_id` в картах/транзакциях/услугах).
- Репозиторий `internal/repository/vendors` (таблицы БД `emitents` / `emitent_*` без переименования колонок), usecase и gRPC/HTTP контроллер обновлены; e2e типы и хелперы под новые эндпоинты.
- Mockery и swagger-теги (12. Вендоры).
