# 2026-03-24

## Работа (transgorobot)

- Изучено логирование в `gpn-online.vip.go` (logrus + `NewRotatingLogger`, структурированные поля, длительность запросов).
- В `newppr.go`: добавлены `newpprLogger`, хелпер полей, логи HTTP в `sendRequestPPR` (duration, status, тело запроса с маскировкой token), авторизация, транзакции (ЛК и public API), смена лимитов/статусов.
- В `newppr_api.go`: `newpprAPILogger`, логи в `sendRequestPPRAPI` и в сценариях смены статуса/лимитов.

- Licard API: разбор ошибки pkcs12 «sequence truncated»; загрузка P12 из `sert_path`, устойчивый base64 и понятные сообщения об ошибках в `loadLicardPKCS12Bytes` / `makeRequesLicard`.

- PPR API: ответ `BAD_REQUEST` / «Same status provided!» обрабатывается как успех (`UpdateExchange` + Info в логе вместо Error).

- Licard: при пустых `sert` и `sert_path` — не Fatal, вывод в консоль контекста и `return nil, 0` из `makeRequesLicard`.
