count|sender_operator_code|
-----+--------------------+
16589|                    |
   59|2AL                 |
    4|2BE                 |
  207|2BK                 |
    2|2HX                 |
    2|2IM                 |
    9|2LD                 |
  407|2VO                 |

```plum

@startuml doc_reader
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Sequence.puml

Container_Boundary(operator_roseu, "operator-roseu")
  Component(ls_handler, "LS Handler", "Обработчик входящей ЛС")
  Component(doc_handler, "Doc Handler", "Обработчик документа")
  Component(user_check, "User Check", "Проверка пользователей")
  Component(docflow_validator, "Docflow Validator", "Проверка Документооборота")
  Component(sign_resolver, "Sign Resolver", "Проверка подписей")
  Component(checker_validator, "Checker Validator", "Валидация сертификатов подписей")
  Component(roseu.po_creator, "ПО Creator", "формирует ПодтверждениеОператора для определенных типов документов")
  Component(roseu.ls_creator, "Сборщик исходящей ЛС [roseu_wq]", "Формирует файл описания ЛС для пакета документов")

  Component(inv_dispatcher, "InvDispatcher", "Обработчик приглашения")
  Component(roseu.receipt_creator, "ТК Creator", "Формирует и упаковывает исходящую ТК на ЛС")
Boundary_End()
    
ContainerDb(db, "База данных", "PostgreSQL")

Container_Boundary(file_system, "FileSystem")
  Component(hot_storage, "/mnt/hot/temporary", "Горячее хранилище распакованных ЛС и документов")
Boundary_End()


Container(rnpt, "rnpt-informer", "Go app", "Проверка, валидация и отправка кодов прослеживаемости в ФНС")
Container(trt, "tech-receipt-tracker", "Go app", "Отслеживание транспортных квитанций")
Container(astral, "operator-astral", "Go app", "Обработка пакетов и дкоументов для нативных клиентов")
Container(reg, "operator-reg", "Go app", "Обработка регистраций")
Container_Ext(flk, "ФЛК (Feature toggle)", "API", "Проверка документов абонентов")

Rel(ls_handler, db, "загрузка задачи из бд", "roseu_rq")
Rel(db, ls_handler, "")
Rel(ls_handler, db, "проверка на дубли по ид ЛС", "таблица ls")
Rel(db, ls_handler, "")
Rel(ls_handler, hot_storage, "загрузка описания description.xml с диска")
Rel(hot_storage, ls_handler, "")

Rel(ls_handler, doc_handler, "если пришёл документ")
Rel(doc_handler, user_check, "проверка пользователей")
Rel(user_check, db, "загружает абонентов по ИдЭДО")
Rel(db, doc_handler, "")

Rel(doc_handler, reg, "если пришёл документ с типом Регистрация", "Posgtres Queue")
Rel(doc_handler, inv_dispatcher, "если пришёл документ с типом Приглашение или ОтветНаПриглашение")

Rel(doc_handler, docflow_validator, "валидация ДО")
Rel(docflow_validator, db, "поиск первичного документа, если пришёл ответный титул")
Rel(db, doc_handler, "")
Rel(doc_handler, db, "сохранение документа в бд", "таблица docs")
Rel(db, doc_handler, "")
Rel(doc_handler, hot_storage, "сохранение документа, подписей и доп. файлов на диск")
Rel(hot_storage, doc_handler, "")

Rel(doc_handler, sign_resolver, "проверка связки подписей к документам")
Rel(sign_resolver, doc_handler, "")

Rel(doc_handler, checker_validator, "валидация сертификатов подписей")
Rel(checker_validator, doc_handler, "")
Rel(doc_handler, flk, "(toggle on) валидация xml структуры формализованных документов")
Rel(flk, doc_handler, "")
Rel(doc_handler, rnpt, "проверка прослеживаемости для первичных и ответных титулов")
Rel(rnpt, doc_handler, "")
Rel(doc_handler, roseu.po_creator, "создание подтверждения Оператора при необходимости")
Rel(roseu.po_creator, doc_handler, "")
Rel(doc_handler, db, "сохранение информации об МЧД (при наличии)", "таблица poa_meta")
Rel(db, doc_handler, "")
Rel(doc_handler, astral, "если пакет нужно отправить нативному клиенту", "Postgres Queue")
Rel(astral, doc_handler, "")
Rel(doc_handler, ls_creator, "если пакет нужно отправить клиенту другого Оператора (тип ПО 100 или 11)", "Postgres Queue")
Rel(ls_creator, doc_handler, "")
Rel(doc_handler, db, "подтверждение обработки документов в БД", "update docs set processed = now() where id = ...")
Rel(db, doc_handler, "")
Rel(doc_handler, astral, "задача на формирование ПДО/ПДП, поиск маркировки", "Postgres Queue")
Rel(astral, doc_handler, "")
Rel(doc_handler, db, "сохранение ЛС в бд", "таблица ls, ls2docs")
Rel(db, doc_handler, "")
Rel(doc_handler, trt, "событие о приёме ЛС", "RabbitMQ Route")
Rel(trt, ls_handler, "")

Rel(ls_handler, roseu.receipt_creator, "формирование ТК на ЛС")
Rel(roseu.receipt_creator, ls_handler, "")
Rel(ls_handler, db, "сохранение результата в бд", "update roseu_rq where id = ...")

@enduml

```